[pytest]
testpaths =
    tests/unit
    tests/e2e
pythonpath = src .
python_files = test_*.py
norecursedirs = archive
addopts = -v --ignore=tests/archive
markers =
    fmp_live: marks tests that call the live FMP API (requires FMP_API_KEY)
